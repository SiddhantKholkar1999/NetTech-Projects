## Https methods

- packages ka aadhar card `package.json`
    - npm init -y (to be used only when package.json is absent)
- json server
  - npm i json-server@0.17.4
- json server auth
  - npm i json-server-auth

## Server Setup

- Create server.js
- Create db.json
- Go to `package.json` and then change from "test": "echo \"Error: no test specified\" && exit 1" to "scripts" section to `"start": "nodemon server.js"`
- To create custom server nodemon
    - npm i nodemon
- npm start 

## Git Activity

- Create `.gitignore` file
- write node_modules